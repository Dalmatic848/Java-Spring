package com.example.Web.App;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class WebAppApplication {

	public static void main(String[] args) {
		SpringApplication.run(WebAppApplication.class, args);
	}

}

//ВСЕ РАБОТАЕТ ПРОСТО ИЗМЕНИ НА СОБСТВЕННУЮ ФОРМУ АВТОРИЗАЦИИ

/*Отлично! Раз преподаватель разрешает делать как хочешь, и ты используешь Spring Boot + Thymeleaf, вот чёткий пошаговый план без кода, что нужно сделать и настроить для выполнения лабораторной работы:

🔹 План выполнения лабораторной работы №6
1. Подключить Spring Security
Добавь зависимость spring-boot-starter-security в pom.xml.
Это включит базовую защиту всех страниц по умолчанию.
2. Настроить пользователей и роли
Создай конфигурационный класс для Spring Security.
Определи в нём:
Пользователей (например, admin и user);
Их пароли;
Роли (например, ADMIN, USER).
Храни данные в памяти (InMemory) — этого достаточно для лабораторной.
3. Настроить HTTP Basic-аутентификацию
В том же конфигурационном классе укажи, что аутентификация должна происходить через HTTP Basic (стандартное окно браузера).
Ограничь доступ к странице /personal-account только авторизованным пользователям (например, с ролью USER).
4. Проверить работу базовой аутентификации
Запусти приложение.
Перейди в браузере на http://localhost:8080/personal-account?name=....
Убедись, что появляется стандартное диалоговое окно для ввода логина и пароля.
Проверь, что:
При правильных данных — страница открывается.
При отмене или неверных данных — доступ запрещён.
5. Настроить SSL (HTTPS)
С помощью утилиты keytool (из JDK) создай самоподписанный сертификат:
Укажи alias (например, tomcat);
Укажи имя файла хранилища (например, keystore.p12);
Задай пароль (не менее 6 символов);
На вопрос "What is your first and last name?" введи localhost.
Положи полученный файл keystore.p12 в папку src/main/resources.
6. Настроить Spring Boot на работу по HTTPS
В файле application.properties укажи:
Порт 8443;
Путь к keystore (classpath:keystore.p12);
Пароль от keystore;
Тип хранилища (PKCS12);
Alias сертификата.
Перезапусти приложение.
7. Проверить работу по HTTPS
Открой в браузере: https://localhost:8443/personal-account?name=....
Убедись, что:
Браузер показывает предупреждение о ненадёжном сертификате;
После подтверждения — снова появляется окно HTTP Basic;
После ввода логина/пароля — страница загружается.
8. Подготовить материалы для отчёта
Собери:

Скриншот консоли с командой keytool (создание сертификата);
Скриншот окна HTTP Basic-аутентификации;
Скриншот предупреждения браузера при заходе по HTTPS;
Скриншот работающего приложения после входа;
Распечатки (текст) следующих файлов:
Конфигурационного класса безопасности (вместо web.xml и tomcat-users.xml);
application.properties (вместо server.xml — настройки SSL).
Готово! Этот план полностью соответствует цели лабораторной работы:
✅ реализация аутентификации (HTTP Basic),
✅ авторизация по ролям,
✅ защита трафика через SSL.

И всё это — в рамках твоего стека (Spring Boot + Thymeleaf). */